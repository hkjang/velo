apiVersion: v1
kind: Secret
metadata:
  name: velo-secrets
  namespace: velo
  labels:
    app.kubernetes.io/name: velo-was
type: Opaque
stringData:
  # AI Provider API Keys — replace with your actual keys
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  # Admin password (base64 encoded in production)
  ADMIN_PASSWORD: "admin"
